{
  "name": "a-bot",
  "version": "1.0.0",
  "main": "src/bot.js",
  "license": "MIT",
  "scripts": {
    "lint": "eslint --quiet --cache \"src/**/*.js\"",
    "dev": "nodemon . --exec \"yarn lint && node\"",
    "deploy-commands": "node src/lib/deploy-commands.js"
  },
  "dependencies": {
    "@discordjs/builders": "^0.11.0",
    "@discordjs/rest": "^0.2.0-canary.0",
    "discord-api-types": "^0.26.1",
    "discord.js": "^13.6.0",
    "dotenv": "^14.0.1"
  },
  "devDependencies": {
    "eslint": "^8.7.0",
    "nodemon": "^2.0.15"
  }
}
